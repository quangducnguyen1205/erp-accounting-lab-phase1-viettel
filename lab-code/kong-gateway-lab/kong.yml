_format_version: "3.0"
_transform: true

services:
  - name: tenant-demo-api
    url: http://host.docker.internal:8080
    routes:
      - name: tenant-demo-master-data-api
        paths:
          - /api/master-data
        strip_path: false
        preserve_host: false
        protocols:
          - http

  - name: tenant-demo-health
    url: http://host.docker.internal:8080/actuator/health
    routes:
      - name: tenant-demo-health-route
        paths:
          - /tenant-demo/actuator/health
        strip_path: true
        preserve_host: false
        protocols:
          - http

plugins:
  - name: cors
    route: tenant-demo-master-data-api
    config:
      origins:
        - http://localhost:5173
      methods:
        - GET
        - POST
        - PUT
        - PATCH
        - DELETE
        - OPTIONS
      headers:
        - Authorization
        - Content-Type
        - X-Request-Id
      exposed_headers:
        - X-Request-Id
      credentials: false
      max_age: 3600
