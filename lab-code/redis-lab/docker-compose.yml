services:
  redis:
    image: redis:7.4-alpine
    container_name: viettel-redis
    command: ["redis-server", "--appendonly", "no"]
    ports:
      - "16379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
