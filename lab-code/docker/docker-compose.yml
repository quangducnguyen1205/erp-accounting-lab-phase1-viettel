
services:
  postgres:
    image: postgres:16
    container_name: tenant-demo-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: tenant_demo
      POSTGRES_USER: demo
      POSTGRES_PASSWORD: demo123
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
